[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "saliency-kyiv"
version = "0.1.0"
description = "CATALINA/GWT Kyiv shelter-seeking simulation — same engine as the instrument"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
include = ["saliency*"]
